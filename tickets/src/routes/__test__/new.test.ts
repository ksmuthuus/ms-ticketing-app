import request from "supertest";
import { app } from "../../app";

it("valid POST ticket endpoint exits", () => {});

it("accessible only with a valid auth token", () => {});

it("error resposne for invalid title", () => {});

it("error response for invalid ticketid", () => {});

it("success response for valid inputs", () => {});
